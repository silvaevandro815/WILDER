version: '3.8'

services:
  wilder-app:
    build: .
    restart: always
    ports:
      - "5000:5000"
    expose:
      - "5000"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:5000/ || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 25s
